[project]
name = "example-upstream"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.6",
]

[dependency-groups]
dev = [
    "black>=26.3.1",
    "flake8>=7.3.0",
]
